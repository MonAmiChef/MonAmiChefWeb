generator client {
  provider = "prisma-client-js"
  output   = "./generated/auth" // optional separate client
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["auth"]  // read-only
}
