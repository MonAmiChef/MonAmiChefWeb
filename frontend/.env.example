# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API Configuration
# In development: Uses local Netlify Dev server (http://localhost:8888)
# In production: Uses deployed backend automatically
# No additional configuration needed - handled by import.meta.env.DEV

# Stripe Configuration
# Customer Portal URL - Get from: https://dashboard.stripe.com/settings/billing/portal
# Falls back to test URL if not set
VITE_STRIPE_CUSTOMER_PORTAL_URL=https://billing.stripe.com/p/login/test_your_portal_link

# Note: Stripe API keys (SECRET_KEY, WEBHOOK_SECRET) are configured in:
# Supabase Dashboard > Edge Functions > Environment Variables
# NEVER add Stripe secret keys to your frontend .env file for security reasons